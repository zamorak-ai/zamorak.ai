# GitHub Pages runs this through Jekyll. The pages are plain HTML without front
# matter, so Jekyll only copies them; these paths stay out of the published site.
exclude:
  - README.md
  - tools/

# Jekyll skips dot-directories unless told otherwise; this one holds security.txt (RFC 9116).
include:
  - .well-known
